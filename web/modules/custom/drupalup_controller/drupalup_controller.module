<?php
/**
 * @File
 * Example custom module for Drupal 8.
 * @author eWeb
 */

/**
 * Implementing hook_menu().
 */
function drupalup_controller_theme($existing, $type, $theme, $path) {
  	return array('article_list' => 
  		    array('variables' => array('items' => array(), 'title' =>'')	
  ) );
}